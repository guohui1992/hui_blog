
 1. 恢复官方地址
 ```shell
git remote set-url origin https://github.com/guohui1992/hui_blog.git
```
2.改用ssh
切换远程为 SSH 地址：
 ```shell
git remote set-url origin git@github.com:guohui1992/hui_blog.git
 ```
测试 SSH 连通性：
 ```shell
ssh -T git@github.com 
 ```
若超时，说明 SSH 端口 22 也被封锁。

GitHub 提供了 SSH over HTTPS 端口 443 的备用入口。
步骤：
编辑或创建 SSH 配置文件：
 ```shell
notepad ~/.ssh/config

添加以下内容：

Host github.com
  Hostname ssh.github.com
  Port 443
  User git

测试：
ssh -T git@github.com
 ```
应该能成功。